# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Test / data
.pytest_cache/
data/
*.parquet
.DS_Store

# Personal workspace — local-only working directories
# `decks/sample_deck.txt` is the public sample (tracked); everything else
# under decks/ is personal and ignored.
decks/*
!decks/sample_deck.txt
prices/
notes/

# Stale Chinese personal notes (kept locally, not for public repo)
google_sheet.md
summary.md
